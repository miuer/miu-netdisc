syntax = "proto3";

option go_package = "micro-storage/service/account/proto;user";

service UserService {
  rpc Login(ReqLogin) returns (RespLogin) {}
}

message ReqLogin { string username = 1; }

message RespLogin { string message = 1; }